apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node20-app.fullname" . }}-test-connection"
  labels:
    {{- include "node20-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node20-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
